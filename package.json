{
  "name": "credit-card-spend-analyzer",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @cardlens/web",
    "build": "npm run build -w @cardlens/web",
    "lint": "npm run lint -w @cardlens/web"
  }
}